# 深入拓展 OAuth

在本节中，我们将从 OAuth 核心协议抽身，放眼围绕这一坚实内核构建的扩展、配置文件（profile）以及各类互补组件的生态。内容涵盖令牌格式、令牌管理、客户端注册、用户认证、面向垂直领域的特定配置（profiling），以及持有证明（Proof of Possession，PoP）令牌等。如果你想进一步了解 OpenID Connect、UMA 或 PoP，本节会带你深入这些主题。上述每个话题几乎都足以单独写成一本书，但我们希望提供足够的信息，帮助你快速上手。
